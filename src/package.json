{
  "name": "photoframebd-render-entry",
  "private": true,
  "description": "Render entry when Root Directory is set to src",
  "scripts": {
    "postinstall": "cd .. && node scripts/render-postinstall.js",
    "start": "node ../dist/index.js"
  }
}
