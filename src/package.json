{
  "name": "photoframebd-render-entry",
  "private": true,
  "description": "Used when Render Root Directory is set to src",
  "scripts": {
    "postinstall": "cd .. && npm install --include=dev && npm run build",
    "start": "node ../dist/index.js"
  }
}
